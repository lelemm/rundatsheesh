name: Release Guest Images (Debian Bash)

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  guest-images-debian-bash:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set tag version
        run: echo "TAG_VERSION=${{ github.event.release.tag_name }}" >> "$GITHUB_ENV"

      - name: Detect changes
        id: changes
        run: |
          set -euo pipefail
          CURRENT_TAG="${{ github.event.release.tag_name }}"
          PREV_TAG=$(git tag --sort=-v:refname | grep -v "^${CURRENT_TAG}$" | head -n1 || true)
          
          if [ -z "$PREV_TAG" ]; then
            echo "No previous tag found, building guest image"
            echo "changed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          
          echo "Comparing $PREV_TAG to $CURRENT_TAG"
          if git diff --name-only "$PREV_TAG" "$CURRENT_TAG" -- \
            services/guest-image/ services/guest-agent/ | grep -q .; then
            echo "Changes detected"
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "No changes detected, skipping build"
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build Debian guest image (bash)
        if: steps.changes.outputs.changed == 'true'
        run: SHELL_VARIANT=bash ./services/guest-image/build.sh

      - name: Package guest image
        if: steps.changes.outputs.changed == 'true'
        run: |
          mkdir -p dist/release
          zip -j "dist/release/guest-image-debian-bash-${TAG_VERSION}.zip" \
            services/guest-image/dist/vmlinux \
            services/guest-image/dist/rootfs.ext4

      - name: Upload release asset
        if: steps.changes.outputs.changed == 'true'
        uses: softprops/action-gh-release@v2
        with:
          files: dist/release/guest-image-debian-bash-*.zip
